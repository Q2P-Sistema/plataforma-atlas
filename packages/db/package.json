{
  "name": "@atlas/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "studio": "drizzle-kit studio"
  },
  "description": "Migrations centralizadas + contratos de views do schema shared",
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.13.0",
    "@atlas/core": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.0",
    "@types/pg": "^8.11.0"
  }
}
