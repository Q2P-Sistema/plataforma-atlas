# Dependências
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Turborepo
.turbo/

# Ambiente
.env
.env.*
!.env.example
.envrc
!.envrc.example
# Env preenchido com segredos reais (deploy) — nunca versionar
Atlas.env
# Stack de UAT — gerenciada no Portainer, não versionar (referência local só)
deploy/portainer/atlas-uat.stack.yml

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Coverage
coverage/
.nyc_output/

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# Vite / React
apps/web/dist/

# Ignorar arquivos de vibecoding legado até importarmos
/legacy/

# Portainer deploy env (contains secrets)
deploy/portainer/*.env

# Documentos PDF não são versionados (fonte .md fica no repo; PDFs são gerados/recebidos)
docs/**/*.pdf
